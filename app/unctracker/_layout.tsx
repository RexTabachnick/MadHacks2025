import { Stack } from "expo-router";
import { styles } from "../stylesheet";
const UncTrackerLayout = () => {
    return <Stack
        screenOptions={{
            headerShown: false
        }}
    />
};

export default UncTrackerLayout;